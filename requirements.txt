# Runtime dependencies (Windows target)
requests>=2.31.0
pyautogui>=0.9.54
pillow>=10.0.0
pynput>=1.7.6
keyboard>=0.13.5
pyttsx3>=2.90
SpeechRecognition>=3.10.0
PyAudio>=0.2.13         # mic capture for SpeechRecognition. If pip fails on Windows, see README.
keyring>=24.0.0
pywin32>=306; sys_platform == "win32"

# Build-only dependency (not needed at runtime)
pyinstaller>=6.0.0
